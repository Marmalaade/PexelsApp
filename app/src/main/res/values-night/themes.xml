<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PexelsApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/red_primary</item>
        <item name="colorOnBackground">@color/black_primary</item>
        <item name="colorPrimaryVariant">@color/dark_gray_dark_palette</item>
        <item name="colorOnPrimary">@color/white_primary</item>
        <item name="colorSecondary">@color/gray_dark_palette</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:fontFamily">@font/mulish</item>
    </style>
</resources>