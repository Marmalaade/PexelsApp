<resources>
    <!-- Base application theme. -->
    <style name="Theme.PexelsApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/red_primary</item>
        <item name="colorOnBackground">@color/white_primary</item>
        <item name="colorPrimaryVariant">@color/gray_light_palette</item>
        <item name="colorOnPrimary">@color/black_primary</item>
        <item name="colorSecondary">@color/dark_gray_light_palette</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:fontFamily">@font/mulish</item>
    </style>

    <style name="Theme.PexelsApp.Launcher" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_logo</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

</resources>